import React from "react";
import ReactDOM from "react-dom/client";
import { TypeaheadBasic } from "./TypeaheadBasic";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <TypeaheadBasic />
  </React.StrictMode>
);
